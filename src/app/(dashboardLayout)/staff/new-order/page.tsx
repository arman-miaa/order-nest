import { RoleRoutePage } from "@/components/shared/role-route-page";

export default function StaffNewOrderPage() {
  return (
    <RoleRoutePage
      role="staff"
      title="New Order"
      description="Create a new order directly for a table or guest."
    />
  );
}