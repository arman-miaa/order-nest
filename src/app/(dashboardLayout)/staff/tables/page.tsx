import { RoleRoutePage } from "@/components/shared/role-route-page";

export default function StaffTablesPage() {
  return (
    <RoleRoutePage
      role="staff"
      title="Table Selection"
      description="Choose and assign tables during the service workflow."
    />
  );
}