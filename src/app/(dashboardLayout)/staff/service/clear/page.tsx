import { RoleRoutePage } from "@/components/shared/role-route-page";

export default function StaffServiceClearPage() {
  return (
    <RoleRoutePage
      role="staff"
      title="Clear Table"
      description="Mark a table as cleared and ready for the next seating."
    />
  );
}