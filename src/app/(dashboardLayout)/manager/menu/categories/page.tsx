import { RoleRoutePage } from "@/components/shared/role-route-page";

export default function ManagerMenuCategoriesPage() {
  return (
    <RoleRoutePage
      role="manager"
      title="Categories"
      description="Organize menu items into categories for easier navigation."
    />
  );
}